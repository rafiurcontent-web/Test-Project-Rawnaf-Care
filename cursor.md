# RAWNAF CARE

Project documentation for Cursor AI.

## Overview

This is the **RAWNAF CARE** project workspace — a **Node.js** application written in **TypeScript**.

## Tech Stack

- **Runtime:** Node.js
- **Language:** TypeScript
- **Package Manager:** npm (or yarn/pnpm — to be decided)
- **Build:** `tsc` (TypeScript compiler)
- **Dev:** `ts-node` / `tsx` for local development (optional)

## Project Structure

```
TEST PROJECT -RAWNAF CARE/
├── src/              # TypeScript source files
├── dist/             # Compiled JavaScript output
├── package.json
├── tsconfig.json
└── cursor.md
```

## Development Notes

- Source code lives in `src/`; compiled output goes to `dist/`.
- Use strict TypeScript settings where practical.
- Document API routes, env variables, and folder conventions as the project grows.

## Cursor AI Guidelines

- Follow existing code style and naming conventions.
- Keep changes focused and minimal.
- Prefer simple, readable solutions over over-engineering.
- Match Node.js + TypeScript patterns already used in the codebase.
